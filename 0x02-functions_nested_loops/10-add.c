#include "main.h"
#include <stdio.h>

/**
 * add - adds two integers and returns the result
 *
 * @a: takes int
 *
 * @b: takes int
 *
 * Return: int
 */
int add(int a, int b)
{
	int result = a + b;

	return (result);
}
